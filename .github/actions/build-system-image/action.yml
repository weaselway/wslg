name: Build the WSLg system image
description: >
  Runs build-and-export.sh, leaving system_x64.tar and system_x64.vhd in the
  workspace root.

runs:
  using: composite
  steps:
    - name: Build and export the VHD
      shell: bash
      run: ./build-and-export.sh
